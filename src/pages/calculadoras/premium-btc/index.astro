---
import BaseLayout from "@layouts/BaseLayout.astro";
import BTCPremiumIndex from "@components/BTCPremiumIndex.astro";
import { webAppSchema, faqSchema, breadcrumbSchema } from "@lib/schema";

const title = "Premium BTC Argentina y Venezuela 2026 – Índice de Brecha | free-bitcoin.lat";
const description =
  "Índice de premium Bitcoin para Argentina (BTC/ARS vs dólar cripto) y Venezuela (USDT/VES vs BCV). Datos en tiempo real y explicación del concepto.";
const canonical = "https://free-bitcoin.lat/calculadoras/premium-btc/";

const faqs = [
  {
    q: "¿Qué es el premium del Bitcoin en Argentina?",
    a: "El 'premium' es la diferencia porcentual entre el precio de Bitcoin calculado al tipo de cambio oficial (dólar MEP o CCL) y el precio real al que se cotiza en exchanges locales. Un premium alto indica que el mercado informal valúa el BTC por encima del precio en dólares, reflejando la demanda de dólares o activos duros fuera del sistema oficial.",
  },
  {
    q: "¿Por qué existe un premium en Venezuela?",
    a: "Venezuela tiene múltiples tipos de cambio: el oficial del BCV (Banco Central de Venezuela), el paralelo (monitordolarvenezuela.com y similares) y el mercado cripto. La brecha surge porque el bolívar pierde valor constantemente y la demanda de USDT o BTC como reserva de valor es alta. El premium refleja la prima por liquidez y por evadir controles cambiarios.",
  },
  {
    q: "¿Es legal comprar Bitcoin aprovechando el premium en Argentina?",
    a: "La compra de Bitcoin es legal en Argentina. El denominado 'dólar cripto' o 'CCL cripto' se mueve en zonas grises regulatorias que han cambiado con cada administración. La AFIP y el BCRA emiten normas periódicas. Consulta siempre el estado regulatorio actualizado y declara tus tenencias conforme a las normas vigentes.",
  },
  {
    q: "¿Cómo se calcula el índice de premium?",
    a: "Premium (%) = ((Precio BTC en moneda local / Tipo de cambio de referencia) / Precio BTC en USD - 1) × 100. Por ejemplo: si BTC vale $10.000.000 ARS, el dólar CCL está a $1.000 ARS y BTC en USD vale $90.000, el precio implícito es $10.000 USD y el premium es ($10.000 / $90.000 - 1) × 100 = +11,1%.",
  },
];

const jsonLd = [
  webAppSchema({
    name: "Índice Premium BTC Argentina Venezuela",
    description,
    url: canonical,
  }),
  faqSchema(faqs),
  breadcrumbSchema([
    { name: "Inicio", item: "https://free-bitcoin.lat/" },
    { name: "Calculadoras", item: "https://free-bitcoin.lat/calculadoras/" },
    { name: "Premium BTC", item: canonical },
  ]),
];
---

<BaseLayout {title} {description} {canonical} {jsonLd}>
  <div class="container">
    <nav class="breadcrumb-nav" aria-label="Miga de pan">
      <a href="/">Inicio</a> › <a href="/calculadoras/">Calculadoras</a> › <span>Premium BTC</span>
    </nav>

    <p class="last-updated">Actualizado: febrero 2026</p>
    <h1>Premium BTC Argentina y Venezuela 2026</h1>

    <p class="lead">
      En países con controles cambiarios o alta inflación como Argentina y Venezuela, el precio de
      Bitcoin en moneda local no siempre refleja el precio internacional multiplicado por el tipo
      de cambio oficial. Esta <strong>brecha o premium</strong> es un termómetro del estrés
      monetario y de la demanda de activos duros fuera del sistema bancario.
    </p>

    <!-- Argentina section -->
    <section class="section" aria-labelledby="ar-heading">
      <h2 id="ar-heading">Argentina: Premium BTC/ARS</h2>
      <p>
        En Argentina conviven varios tipos de cambio: el oficial, el blue (mercado paralelo), el
        MEP y el Contado con Liquidación (CCL). El precio de Bitcoin en exchanges locales suele
        tener un "premium" sobre el precio internacional multiplicado por el dólar MEP, reflejando
        la demanda de activos que escapan al cepo cambiario.
      </p>
      <BTCPremiumIndex country="ar" />
    </section>

    <!-- Venezuela section -->
    <section class="section" aria-labelledby="ve-heading">
      <h2 id="ve-heading">Venezuela: Premium BTC/VES y USDT/VES</h2>
      <p>
        En Venezuela, el bolívar soberano (VES) pierde valor constantemente. El mercado cripto
        sirve como refugio: USDT y Bitcoin cotizan con un premium respecto al tipo de cambio
        oficial del BCV. Este índice muestra la brecha entre el mercado cripto y el tipo de cambio
        de referencia.
      </p>
      <BTCPremiumIndex country="ve" />
    </section>

    <!-- Explanation section -->
    <section class="section" aria-labelledby="explain-heading">
      <h2 id="explain-heading">¿Cómo interpretar el índice de premium?</h2>

      <div class="table-wrap">
        <table>
          <thead>
            <tr>
              <th>Nivel de premium</th>
              <th>Interpretación</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <td><span class="badge badge-green">0% – 5%</span></td>
              <td>Mercado relativamente normalizado. Poca brecha entre mercado formal e informal.</td>
            </tr>
            <tr>
              <td><span class="badge badge-yellow">5% – 15%</span></td>
              <td>Tensión cambiaria moderada. Mayor demanda de dólares o activos dolarizados.</td>
            </tr>
            <tr>
              <td><span class="badge badge-orange">15% – 30%</span></td>
              <td>Fuerte presión sobre la moneda local. Posibles restricciones cambiarias nuevas.</td>
            </tr>
            <tr>
              <td><span class="badge badge-red">&gt; 30%</span></td>
              <td>Crisis cambiaria severa. Demanda urgente de activos fuera del sistema bancario local.</td>
            </tr>
          </tbody>
        </table>
      </div>

      <p>
        Un premium negativo (precio de BTC por debajo del precio internacional) es inusual y puede
        indicar restricciones a las salidas de capital, liquidez excesiva en el mercado local o
        manipulación de precios en exchanges con bajo volumen.
      </p>
    </section>

    <!-- FAQ -->
    <section class="section" aria-labelledby="faq-heading">
      <h2 id="faq-heading">Preguntas frecuentes sobre el premium BTC</h2>
      <div class="faq" role="list">
        {
          faqs.map((item, i) => (
            <div class="faq-item" role="listitem">
              <button class="faq-question" aria-expanded="false" data-faq-trigger aria-controls={`prem-faq-${i}`}>
                <span>{item.q}</span>
                <svg class="chevron" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" aria-hidden="true">
                  <polyline points="6 9 12 15 18 9" />
                </svg>
              </button>
              <div class="faq-answer" id={`prem-faq-${i}`} role="region" aria-label={item.q} set:html={item.a} />
            </div>
          ))
        }
      </div>
    </section>

    <p class="disclaimer">
      Los datos de precios y tipos de cambio son referenciales y pueden diferir de los valores
      reales en exchanges o mercados informales. Esta herramienta es solo educativa y no constituye
      asesoría financiera ni cambiaria.
    </p>
  </div>
</BaseLayout>

<style>
  h1 {
    font-size: clamp(1.8rem, 4vw, 2.4rem);
    margin: var(--space-4) 0;
  }

  h2 {
    font-size: clamp(1.3rem, 2.5vw, 1.7rem);
    margin-bottom: var(--space-4);
    color: var(--c-primary);
  }

  .lead {
    font-size: 1.05rem;
    color: var(--c-text-muted);
    margin-bottom: var(--space-6);
    max-width: 720px;
  }

  p {
    color: var(--c-text-muted);
    margin-bottom: var(--space-4);
  }

  .section {
    margin: var(--space-10) 0;
    padding-top: var(--space-2);
    border-top: 1px solid var(--c-border);
  }

  .breadcrumb-nav {
    font-size: 0.85rem;
    color: var(--c-text-muted);
    margin: var(--space-4) 0 var(--space-2);
  }

  .breadcrumb-nav a {
    color: var(--c-text-muted);
    text-decoration: none;
  }

  .breadcrumb-nav a:hover {
    color: var(--c-primary);
  }

  table {
    width: 100%;
    border-collapse: collapse;
    font-size: 0.9rem;
  }

  th,
  td {
    padding: var(--space-3) var(--space-4);
    border: 1px solid var(--c-border);
    text-align: left;
  }

  th {
    background: var(--c-surface-1);
    color: var(--c-text);
    font-family: var(--font-display);
    font-weight: 600;
  }

  td {
    color: var(--c-text-muted);
  }

  .badge-green { background: rgba(0, 200, 100, 0.15); color: #00c864; }
  .badge-yellow { background: rgba(255, 200, 0, 0.15); color: #ffc800; }
  .badge-orange { background: rgba(255, 107, 0, 0.15); color: var(--c-primary); }
  .badge-red { background: rgba(255, 50, 50, 0.15); color: #ff3232; }
</style>

<script>
  function initFAQ() {
    document.querySelectorAll<HTMLButtonElement>("[data-faq-trigger]").forEach((t) => t.replaceWith(t.cloneNode(true)));
    document.querySelectorAll<HTMLButtonElement>("[data-faq-trigger]").forEach((trigger) => {
      trigger.addEventListener("click", () => {
        const item = trigger.closest(".faq-item");
        if (!item) return;
        const isOpen = item.classList.contains("open");
        const answer = document.getElementById(trigger.getAttribute("aria-controls") ?? "");
        if (isOpen) {
          item.classList.remove("open");
          trigger.setAttribute("aria-expanded", "false");
          if (answer) answer.style.display = "none";
        } else {
          item.classList.add("open");
          trigger.setAttribute("aria-expanded", "true");
          if (answer) answer.style.display = "block";
        }
      });
    });
  }
  initFAQ();
  document.addEventListener("astro:page-load", initFAQ);
</script>
